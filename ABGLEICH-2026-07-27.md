# Soll-Ist-Abgleich, 27.07.2026

Prüfstand: HEAD `d7a7c9f`, Arbeitsbaum sauber, `index.html` 6301 Zeilen.
Prüfauftrag nach `superduper-abgleich-prompt.md`. Es wurde nichts geändert, nichts committet, nichts gepusht. Diese Datei ist das einzige Ergebnis.

## Nachtrag, 27.07.2026 nachmittags

Die neun Funde der Stufe "bricht etwas" sind seit diesem Bericht behoben, in zwei Commits direkt im Anschluss an die Prüfung:

* **F1 bis F8** in einem Commit an `index.html`. Verifiziert mit Syntaxcheck, 300-Frame-Soak und Browser-Introspektion; F4, F6 und F7 zusätzlich zur Laufzeit simuliert (Grüßen wird in der Kammer angeboten, die Randnotiz weicht dem Dienstzettel, 1200 gewürfelte Kammertüren landeten null mal im Dorf).
* **F9** durch Umbau des Pages-Wegs: die Kenmi-Grafik liegt jetzt im privaten Repo `wurstbrotdlx/superduper-adventure-assets`, `.github/workflows/pages.yml` lädt sie per read-only Deploy Key und deployt über `actions/deploy-pages`. `docs/` ist aus dem Tracking und per `git-filter-repo` aus der gesamten Historie entfernt, `main` wurde einmalig force-gepusht. Ein frischer Klon enthält kein einziges PNG-Byte mehr, `.git` schrumpfte von 5,0 auf 1,0 MB. Damit ist auch **F80** (linear wachsendes Base64-Artefakt) erledigt und **F44** teilweise, weil der Build-Schritt jetzt automatisch läuft statt von Hand.

Alles darunter, also **F10 bis F83**, ist unverändert offen. Die Vorschlagsliste am Ende gilt weiter, abzüglich der oben genannten Punkte. Die beiden Einschränkungen im Abschnitt "Ungeprüft" gelten ebenfalls unverändert: die adversariale Gegenprobe ist ausgefallen, und die systematische Zusagen-Bilanz aus Abschnitt 5 des Auftrags fehlt.

Achtung für spätere Leser: durch den History-Rewrite haben alle Commits ab `68b97b7` neue Hashes. Die in diesem Bericht genannten Commit-IDs beziehen sich auf den Stand vor dem Rewrite.

## Ampel

**Git:** sauber und synchron, nichts uncommittet, nichts ungepusht, GitHub Pages byte-identisch mit `docs/index.html`; ein einziger, aber schwerer Befund: der öffentliche Pages-Build trägt 99 lizenzierte Cute-Fantasy-PNGs als data-URIs im Repo.

**Phasenstand:** alle 14 gebauten Abschnitte (Gameplay 1 bis 6, Grafik G0 bis G5, W1, W2) sind real im Code belegbar, kein einziger Fall von "steht ERLEDIGT, ist aber nicht drin"; der Fehler liegt durchweg in der Gegenrichtung, W1 und W2 tragen in keinem Plandokument einen Statusmarker und drei Planabschnitte beschreiben einen Zustand, den G5 längst überholt hat.

**Gesamt:** das Spiel läuft fehlerfrei (Syntaxcheck 0 Fehler, Live-Lauf ohne Konsolenausnahme, 99 von 99 Assets geladen), die Substanz stimmt; die teuren Funde liegen fast alle an einer Naht, nämlich dort, wo G5 ein begehbares Dorf gebaut hat, ohne die Register der abgeschlossenen Phasen 4, 5 und 6 mitzupflegen.

---

## Phasentabelle

| Phase | Marker im Plan | Code vorhanden | Commit | Urteil |
|---|---|---|---|---|
| Gameplay 1, Zutaten-Grammatik | `— ERLEDIGT` (gameplay:55) | ja: `ZUTAT_NOUNS` 21, `ZUTAT_ADJ` 26, `WIRKUNG` 24, `CRAFT_BASE` | `ab53cae`, erweitert `b9da66a` | erfüllt, alle Tabellenlängen mechanisch nachgezählt |
| Gameplay 2, Kammern mit Preisschild | `— ERLEDIGT` (gameplay:101) | ja: `KAM_MOD` mit 8 Modulen, `betreteKammer`, `verlasseKammer` | `b9da66a` | erfüllt, Lösbarkeit je Modul laufzeitgebunden |
| Gameplay 3, Fluch-Ökonomie | `— ERLEDIGT` (gameplay:149) | ja: `FLUCH` 24, `item.fluchRuht`, 6 harte Flüche | `9633b8c` | erfüllt, aber ein Fluch bricht Phase 2 (F4) |
| Gameplay 4, Dienst nach Vorschrift | `— ERLEDIGT` (gameplay:187) | ja: `startShift`, `endShift`, `AUSBAU_DEFS`, `JAHRES_BONI` | `55236b8` | erfüllt, Umsetzungsnotiz seit G5 an zwei Stellen falsch |
| Gameplay 5, Knöterich | `— ERLEDIGT` (gameplay:248) | ja: `KN_KEY`, `HINWEISE` 11, `ESCALATE_DEFS` 6, `knAssertCaps` | `b381eb3`, Marker `1455571` | erfüllt, `knAssertCaps()` läuft zur Laufzeit grün (selbst geprüft) |
| Gameplay 6, Soundtrack | `— ERLEDIGT` (gameplay:497) | ja: `ZONES` mit 6 Zonen, `MUS`, `prepareZone` | `3ac94dd` | erfüllt, aber Stummschaltung defekt (F3) |
| Grafik G0, Fundament | kein Kopfmarker, `(erledigt)` nur bei den Notizen (grafik:181) | nicht in `index.html` (per Auftrag), `tools/sheet-audit.mjs` + `manifest.json` | `2a216b4` | erfüllt über den erlaubten JSON-Zweig, Manifest teils falsch (F15) |
| Grafik G1, Kammern-Interieur | kein Kopfmarker, `(erledigt)` (grafik:220) | ja: `DUN_SET`, `bakeDunTile`, `dun1`/`dun2`-Sheets | `046afce` | erfüllt, 2 statt 3 Sets (Dungeon_3 ist leer, dokumentiert) |
| Grafik G2, Held modular | kein Kopfmarker, `(erledigt)` (grafik:280) | ja: `CF_ANCHOR`, `bakeHeroSheet`, `SHEETS['hero_baked']` | `116d164` | erfüllt für Rüstung/Stiefel, Waffe und Schild erreichen die Figur nicht (F16) |
| Grafik G3, Monster auf echte Rigs | kein Kopfmarker, `(erledigt)` (grafik:417) | ja: `CF_RIGS`, `MONDEF` 21 Typen mit `rig`-Feld | `b4a6bef` | erfüllt, Fledermaus auf Ein-Zeilen-Sheet (F17) |
| Grafik G4, Oberwelt-Tiles | kein Kopfmarker, `(erledigt)` (grafik:562) | ja: `cfgrass1..4`, `TILE_TINT`, `TILE_CACHE`, `G_LAVA` | `770b089` | erfüllt, aber `shadowDirt` beim Umbau verloren (F8) |
| Grafik G5, Dorf/UI/Sunnyside | kein Kopfmarker, `(erledigt)` (grafik:714) | ja: `VILLAGE`, `VILLAGE_BUILDINGS` 6, `NPC_DEFS` 3, `AMT_TUER`, `bakeUiSkin` | `9909c91` + `68b76b7`, `4423f71`, `d7a7c9f` | erfüllt, aber Ursprung der meisten Querstände (F2, F9, F10, F18) |
| W1, Terminologie | **kein Marker**, weder `phase-w1-terminologie.md:1` noch `weltbibel:508` | ja: `<title>`, 21 `art`-Felder in `MONDEF`, Zonen-HUD, `DIENST_BEMERKUNGEN` | `a67c9c3` + Nachzug `1b47e8e` | gebaut und gepusht, steht aber nirgends dran (F22) |
| W2, Aktenfunde | **kein Marker**, weder `phase-w2-aktenfunde.md:1` noch `weltbibel:522` | ja: `BLAETTER` 18 (12 A + 6 B), `renderBlaetter`, Reiter `#tabBlaetter` | `6c116d0` | gebaut und gepusht, steht aber nirgends dran (F22); Reiter zur Laufzeit bestätigt |
| W3, Das Dorf spricht | kein Marker, offen (`weltbibel:531`) | nein: kein `AKT_NPC`, kein Ensemble-Name im Code, NPCs sind Staffage | keiner | korrekt offen, G5 hat die Vorarbeit geliefert |
| W4, Schwarzes Brett | kein Marker, offen (`weltbibel:540`) | nein: 0 Treffer für "Brett", "Auftrag" | keiner | korrekt offen, keine halbe Vorwegnahme |
| W5, Der Vorgang | kein Marker, offen (`weltbibel:549`) | nein: kein Aktstand in `loadAmt`, keine Zustell-Aktion | keiner | korrekt offen |

Gegenprobe in beide Richtungen: kein Abschnitt behauptet Fertigstellung ohne Code. Umgekehrt sind W1 und W2 fertig, ohne dass ein Dokument das sagt.

---

## Funde

Reihenfolge nach Schwere. Die neun Funde der Stufe "bricht etwas" habe ich nach der Meldung selbst am Code gegengeprüft, ebenso die Titel-, Text- und Regressionsfunde. Die übrigen sind Einzelmeldung ohne unabhängige Gegenprobe, siehe Abschnitt "Ungeprüft".

### Bricht etwas

**F1. Jahresbonus "Dienstsiegel" überlebt keinen Reload.**
`index.html:5755` (`JAHRES_BONI`), `index.html:2985` (`saveAmt`), `index.html:2963` (`CONFIG.kammerNachwachsen`).
Vier der fünf Jahresboni schreiben nach `amt` und werden mitgespeichert. Der fünfte schreibt nach `CONFIG.kammerNachwachsen`. `saveAmt()` serialisiert ausschließlich `amt`, `CONFIG` wird nirgends geschrieben oder gelesen. Zur Laufzeit bestätigt: `localStorage['sda_amt_v1']` enthält kein Nachwachs-Feld, `CONFIG.kammerNachwachsen` steht nach 10 gespielten Schichten wieder auf dem Default 120.
Zählt, weil der Bonus als dauerhaft angekündigt ist. Der Bonus-Index läuft zyklisch (`index.html:5870`), der Spieler landet also alle 50 Schichten wieder auf dem einzigen Slot, der nichts liefert. Im Plan als Falle vermerkt (`gameplay:239`), seither unverändert.
Kosten: klein. Feld `amt.bonusNachwachsen` einführen, `apply()` und die Verbrauchsstelle `index.html:3620` umstellen, eine Zeile in `loadAmt`. Etwa 5 Zeilen.

**F2. `CONFIG.schichtModus = false` schaltet nicht mehr sauber zurück.**
`index.html:4008` (`aktBiete(AMT_TUER…, AKT_AMT…)`), `index.html:4061-4063` (`amtFeierabendNehmen`).
Alle Phase-4-Einstiege prüfen die Flagge (`index.html:2742`, `4701`, `5709`). Der von G5 nachgezogene Kontextpunkt am Amtsgebäude prüft nichts: `scanAktion()` bietet `AKT_AMT` bedingungslos an, `amtFeierabendNehmen()` ruft `endShift('amt')` ungeprüft.
Zählt, weil der Spieler mit ausgeschaltetem Schichtmodus am Amt F drücken kann und im vollen Schichtapparat landet, inklusive `amt.schichten++`, Bankbuchung, `showDorf()` und Reset von Level, Skillpunkten und Ausrüstung. Genau das, was die Flagge abschalten soll. Der Bericht zeigt dann zusätzlich "Monster erledigt: 0", weil `shiftKillsTotal` außerhalb des Schichtmodus nie gefüllt wird.
Kosten: eine Zeile. Guard an `index.html:4008` oder bedingtes Rendern des Knopfs in `renderAmtFenster()`.

**F3. Musik-Stummschaltung erfasst nur den Melodiebus.**
`index.html:1021-1025` (`applyMusicGain`), `index.html:1004-1008` (Busgraph), `index.html:1442` (Stinger auf `master`).
`applyMusicGain()` setzt bei `musicMuted` nur `musicBus.gain` auf 0. `kampfGain`, `gefahrGain`, `zoneDroneGain` und `swellGain` hängen als gleichrangige Geschwister direkt an `toFx`, der Knöterich-Stinger spielt sogar an `toFx` vorbei direkt auf `master`. Am Code verifiziert.
Zählt, weil "Aus" nur Lead, Bass und Akkorde stummschaltet. Weiter hörbar bleiben Kampf-Snare (0.3), Kampf-Shaker (0.22), Tritonus-Gefahr-Drone (0.9, also lauter als alles andere im Graph), Zonen-Drone und jeder Stinger. Derselbe Defekt gilt für den Lautstärkeregler. Der Vorzustand war doppelt geblockt, einmal im Takt und einmal am Bus; Phase 6 behauptet in `gameplay:565` ausdrücklich "Verhalten unverändert erhalten".
Kosten: fünf Zeilen. Vier Layer-Gains von `toFx` auf `musicBus` umhängen, Stinger von `master` auf `musicBus`. Danach prüfen, ob der Stinger noch über dem Track steht.

**F4. Fluch "Grußpflicht" macht reguläre Kammerwachen im Nahkampf unverwundbar.**
`index.html:2609` (Gate in `hurtMon`), `index.html:3996-4003` (Angebot nur außerhalb der Kammer), `index.html:2481` (`regel: null`), `index.html:3547` und `3494` (betroffene Monster).
`hurtMon` blockiert jeden Nahkampftreffer, solange `CFX.gruss` aktiv ist und das Ziel weder gegrüßt ist noch eine Kammer-Sonderregel trägt. `AKT_GRUSS` steht ausschließlich im Zweig `if(!kammer)`. `m.regel` wird im gesamten Code nur an einer Stelle gesetzt (`index.html:3839`, Modul "welle"). Die von `setzeWaechter` erzeugten Wachen und der Schatzkammer-Boss `bossgeneric` bekommen keine.
Zählt, weil ein Nahkampf-Build mit angelegter Grußpflicht in der Kammer weder treffen noch grüßen kann. Kein harter Softlock (Zauber wirken, Esc und Ablegen bleiben möglich), aber Abnahme Phase 3 verlangt ausdrücklich, dass kein Fluch das Spiel unspielbar macht, und der Kommentar über `gruessen()` begründet die Beschränkung mit einer Ausnahme, die es für diese Wachen nicht gibt.
Kosten: eine Zeile. Entweder `AKT_GRUSS` auch im Kammer-Zweig anbieten (der Nähe-Guard `index.html:4000-4002` lässt sich unverändert wiederverwenden) oder beim Wachen-Spawn `m.gruss = true` setzen.

**F5. `tuer1` prüft keine Sichtweite: `dist2()` liefert die Distanz, verglichen wird gegen einen Quadratwert.**
`index.html:3035` und `index.html:3079`, Helfer `index.html:918-919`.
Beide `tuer1`-Prädikate rufen `dist2(player.x,player.y,t.x,t.y) < 202500`. `dist2` ist die echte Distanz mit `Math.sqrt`, die quadratfreie Variante heißt `sqDist`. Bei 80x80 Kacheln zu 32px liegt die größte mögliche Distanz bei rund 3620 Pixeln, 202500 wird nie erreicht.
Zählt, weil der Hinweis "Gebührenbescheid lesen. Steht alles drauf." ab dem ersten Frame der Oberwelt feuert, sobald irgendeine Tür bereitsteht, also auch am Kessel ohne sichtbare Tür, und mit Prio 70 alle niedrigeren Katalogzettel verdrängt. Dieselbe Bedingung steht in `ESCALATE_DEFS.tuer1`, die Eskalation ist also dauerhaft scharf. Zusätzlich zieht der Aufruf ein `Math.sqrt` in ein Prädikat, das wurzelfrei sein sollte. Die Umsetzungsnotiz `gameplay:482` behauptet "dist2 < 202500, also 450 Pixel"; das wäre `sqDist`.
Kosten: zwei Bezeichner. `dist2` durch `sqDist` ersetzen. Die übrigen 20 `dist2`-Aufrufe nutzen lineare Schwellen und bleiben unberührt.

**F6. Dienstzettel verdrängt eine laufende Randnotiz nicht.**
`index.html:3176-3198` (`knDisplayZettel`), CSS `index.html:149`.
Die Regel "nie gleichzeitig mit einem Dienstzettel" (`gameplay:403`, Abnahme `:461`) ist nur in einer Richtung verdrahtet: `knShowLineGated` bricht bei `knZettel.active` ab (`index.html:3221`). Der umgekehrte Fall fehlt. Beide Knoten teilen exakt dieselbe Geometrie (`#knZettel,#knRandnotiz{position:fixed;left:50%;top:46px;…}`).
Zählt, weil eine Randnotiz 3 Sekunden hält und `knEvaluateZettel` alle 15 Frames läuft. Fällt ein Zettel in dieses Fenster, liegen zwei absolut positionierte Kästen deckungsgleich übereinander.
Kosten: eine Zeile in `knDisplayZettel`, die `#knRandnotiz` vorher ausblendet.

**F7. Kammertüren können mitten im Dorf stehen.**
`index.html:3393-3412` (`setzeKammerTueren`), Gegenstücke `index.html:2505` und `index.html:1786`.
G5 hat den Dorf-Ausschluss in zwei von drei Platzierungsfunktionen nachgezogen: `placeMonsters()` und der Critter-Spawn bekamen je ein `if(inVillageT(tx,ty)) continue;`. `setzeKammerTueren()` bekam nichts, seine einzige Ortsschranke ist der 12-Kachel-Radius um `SPAWN`. Weil G5 das ganze Dorfrechteck auf `G_PATH` freiräumt und `G_PATH` in `WALKABLE` steht, erfüllt jede Dorfkachel die Bedingung sogar besser als das umgebende Gelände. Der Prüfagent hat es im echten Ladelauf gemessen: 13 von 2400 gewürfelten Türen landeten im Dorfrechteck, konkrete Treffer bei (24,37), (22,33), (24,36), (23,47).
Zählt, weil `setzeKammerTueren()` bei jedem Schichtantritt erneut läuft (`index.html:5791`), also ein Risiko pro Schicht ist. Trifft es, steht ein versiegeltes Kammertor samt Schild und Schwierigkeitsbalken neben Markt oder Amt.
Kosten: eine Zeile, wortgleich zum Vorbild zwei Funktionen weiter. Die Schleife hat 300 Versuche pro Tür, der Ausschluss kostet keine Platzierung.

**F8. Der Schattenland-Weg hat seit G4 gar keine Umfärbung mehr.**
`index.html:1659` (Aufrufstelle), `index.html:1563-1566` (`TILE_TINT`).
`initFloorGraphics()` übergibt für `G_PATH` im Schattenland weiterhin den Tint-Namen `'shadowDirt'`. `TILE_TINT` enthält seit G4 nur noch `snow` und `ice`. Verifiziert: `grep shadowDirt` findet genau einen Treffer, nämlich die Aufrufstelle; der Eintrag existiert nicht mehr. `bakeCfTile()` bekommt `undefined` und backt ungetönt.
Zählt, weil G5 den Schaden vervielfacht hat: die Dorf-Freiräumung setzt das ganze 19x15-Rechteck auf `G_PATH`, statt der früheren kleinen Angerfläche sind es 229 Kacheln. Im Schattenland steht damit ein großer hellbrauner Block mitten im lila Pilzland. Die Umsetzungsnotiz `grafik:647-648` behauptet ausdrücklich das Gegenteil.
Kosten: eine Zeile. `shadowDirt: ['#2e0f42', 0.74]` wieder aufnehmen, danach optisch nachsehen.

**F9. Die lizenzierte Cute-Fantasy-Grafik liegt im öffentlichen Repo, base64-kodiert in `docs/index.html`.**
`docs/index.html` (getrackt seit `68b76b7`), gegen `tools/build-single.mjs:8-9` und `.gitignore:7-21`.
`docs/index.html` ist byte-identisch mit dem ignorierten `dist/index.html` (beide sha256 `8886c3de…`, 1372418 Bytes) und enthält 99 vollständige PNGs als data-URIs, abgelegt unter ihren Originalpfaden als JSON-Schlüssel. Ohne Werkzeug wieder herauszulösen: die Stichprobe `Farmer_Shirt_1_Green.png` dekodiert zu einem gültigen PNG, 576x3584. Das Repo ist öffentlich.
Zählt, weil es die Prämisse zerlegt, auf der die gesamte `.gitignore`-Architektur steht. `tools/build-single.mjs:9` formuliert die Grenze selbst: ausliefern ja, "original benannte PNGs in einem öffentlichen Repo abzulegen ist es nicht". Der Vor-Commit-Check aus `grafik:47` läuft weiter grün durch, weil er Dateinamen prüft, nicht Inhalt. Der Pages-Commit argumentiert bewusst, das sei der gekaufte Anwendungsfall, und diese Lesart ist vertretbar. Aber sie widerspricht der im Repo niedergeschriebenen Auslegung, und keiner der beiden Texte wurde angepasst.
Kosten: entweder eine bewusste Entscheidung im Lizenzabschnitt festschreiben (etwa 15 Minuten), oder Pages auf `gh-pages`-Branch beziehungsweise Actions-Deploy umstellen (etwa 1 Stunde, plus einmalige History-Bereinigung, falls die zwei bereits gepushten Blobs verschwinden sollen).

### Weicht vom Plan ab

**F10. Das G5-Amtsfenster fehlt in allen Panel-Registern der Phasen 4, 5 und 6.**
`index.html:4044` (`amtFensterOpen`), gegen `index.html:3153` (`knSperrzone`), `index.html:3328` (`knNachfragen`), `index.html:4709` (Schichtuhr), sowie fehlender `MUS.muffle`/`MUS.goto('office')`-Aufruf in `amtFensterOeffnen` (`index.html:4045`).
Drei Guard-Listen zählen dieselben vier Flags plus `#overlay` auf, keine kennt `amtFensterOpen`. Verifiziert: `if(invOpen || spellTreeOpen || kesselOpen || schlossOpen)` an beiden Stellen. Die vierte Liste, die CSS-Skin-Schleife `index.html:6264`, kennt `#amtFenster` sehr wohl.
Zählt, weil `state` bei offenem Amtsfenster bewusst auf `'play'` bleibt, `update()` also weiterläuft und ein Dienstzettel über das Panel knallen kann. Ein F-Druck auf "Nachfragen" wird nicht geblockt, `knIdleT` wird beim Öffnen nicht genullt (anders als bei Inventar, Zauberbaum, Kessel), und die Musik dämpft nicht. Klassischer Querstand: eine spätere Phase erweitert eine Kategorie, deren Regeln zwei frühere Phasen als Liste hart codiert haben.
Kosten: je ein Term in drei Listen, plus `knIdleT = 0` beim Öffnen, plus eine Entscheidung über Dämpfung oder `office`-Track.

**F11. Randnotiz-Anlass `untaetigkeit` wird nie ausgelöst.**
`index.html:3099` (toter Pool), fehlender Hook in `knTick` `index.html:3342-3359`.
Der Plan fordert eine Randnotiz nach 25 Sekunden ohne echte Aktion (`gameplay:374`) und regelt ihr Verhältnis zum Steckenbleib-Schubs (`:380`). Der Pool existiert mit allen vier Planzeilen, aber alle sechs `knRandnotiz()`-Aufrufe im Code lauten `crit`, `levelup`, `ultimate`, `fluch`, `goldfund`, `goldfund`. `knIdleT` wird geführt, aber nur von `knStuckCandidate` mit Schwelle 50 gelesen.
Zählt, weil einer von sieben Anlässen ersatzlos fehlt, und zwar der einzige, der ohne Spielerhandlung feuert, also der, der Knöterich im Leerlauf am Leben hält. Der tote Pool wird von `knAssertCaps` mitgeprüft und erweckt den Eindruck, der Kanal sei fertig.
Kosten: klein. Ein Zweig in `knTick`, gedeckelt über das vorhandene 40-Sekunden-Gate.

**F12. Zweiter Steckenbleib-Schubs kommt nach 25 Sekunden ohne Spieleraktion.**
`index.html:3190` (`knDisplayZettel`), `index.html:3166` und `3343`.
`gameplay:346` fordert "Kein zweiter Schubs vor der nächsten echten Aktion". Der Code setzt bei einem Steckenbleib-Kandidaten nur `kn.wissensluecke[…] = true`, `knIdleT` läuft über die 50-Sekunden-Schwelle hinaus weiter, `knStuckCandidate` liefert sofort die nächste offene Wissenslücke, und da Steckenbleib-Zettel `exempt:false` sind, kommt sie nach 25 Sekunden Cooldown.
Zählt, weil ein Spieler, der 100 Sekunden stillsteht, drei Schubse statt einem bekommt und dabei das 3-Zettel-Budget der ersten zwei Minuten verbrennt. Genau das Nerv-Verhalten, gegen das der Absatz geschrieben wurde.
Kosten: eine Zeile, `knIdleT = 0` in `index.html:3190`.

**F13. `unequipItem()` ruft `knCheckFluchEquipped()` nicht, der Flankendetektor bleibt blind.**
`index.html:4353-4364` gegen `index.html:4351` und `index.html:3246-3256`.
`knCheckFluchEquipped` arbeitet auf einer steigenden Flanke gegen `knCurseWasOn` und hängt nur an `equipItemFromBag` und `startShift`. `unequipItem` ruft `recalc()`, aber nicht `knCheckFluchEquipped`. Zieht der Spieler ein verfluchtes Teil aus, bleibt `knCurseWasOn` true.
Zählt, weil beim nächsten Anlegen keine Flanke entsteht: kein `kn.pending.fluch1`, kein `kn.counters.fluchAngelegt++`, keine `fluch`-Randnotiz. Die Regel "ab dem zweiten Vorkommen" (`gameplay:378`) kann dauerhaft unerreichbar bleiben. Die Umsetzungsnotiz `:486` begründet die Auslassung damit, Ausziehen könne keinen neuen Fluch anlegen; das trifft zu, übersieht aber die Blindheit des Detektors.
Kosten: eine Zeile hinter `recalc()`.

**F14. Knöterichs Kopf steht auch im Jahresgespräch, obwohl die Abnahme das ausschließt.**
`index.html:5875` gegen `index.html:5852`.
`gameplay:266` und Abnahme `:463` sagen: Kopf nur im Dienstbericht aus `endShift()`, nicht in `showDorf()` und nicht im Jahresgespräch. Der Dienstbericht ist korrekt, `showDorf()` ist sauber, das Jahresgespräch trägt dasselbe Glyph.
Zählt, weil die Begründung im Plan inhaltlich ist: im Jahresgespräch gehört die Bühne dem Bürgermeister. Nebenbei klebt der Emoji hier im selben `<p>` unmittelbar vor dem Zitat und liest sich als Teil der Figurenzeile, was gegen "Keine Emojis in Figurentexten" (`weltbibel:500`) läuft.
Kosten: ein Zeichen streichen.

**F15. Das G0-Manifest hat bei Animations-Sheets falsche Raster und wurde nie nachgezogen.**
`assets/cf/manifest.json`, Gegenprobe `index.html:755-757`.
`Dungeon_1/2_Gate_anim.png` steht als 16x16 mit 104 Frames, real sind es 26 Frames à 32x32. `Gate_Closed.png` steht als 16x16 mit 4 Frames, ist aber ein einzelnes 32x32-Sprite. `Pillars.png` steht als 16x16 mit 9 Frames, der Code lädt 16x48 mit 3. Der Code liegt jeweils richtig. G1 hat den Fehler erkannt (`grafik:224`), aber weder das Manifest neu erzeugt noch den vorgesehenen Override benutzt: `tools/sheet-audit.overrides.json` erlaubt laut eigenem `_comment` fw/fh-Overrides und enthält bis heute keinen einzigen.
Zählt, weil die Folge im Code steht: G2 und G3 umgehen das Manifest ausdrücklich ("nicht aus dem G0-Manifest übernommen", `index.html:501-503`; "NIE aus assets/cf/manifest.json übernommen", `:566-569`). Zusätzlich sind 447 von 886 Einträgen unter der Confidence-Schwelle, davon 418 nach eigener Angabe ungeprüft. Wer künftig ein Sheet einbaut und dem Manifest glaubt, bekommt denselben Fehler wieder.
Kosten: klein. Drei Override-Paare eintragen, `node tools/sheet-audit.mjs` einmal laufen lassen.

**F16. G2: zwei von vier Ausrüstungs-Slots ändern den Helden gar nicht.**
`index.html:5425-5453` (Waffe und Schild in `drawPlayer`), `index.html:884-899` (Bake-Reihenfolge).
Gebacken werden Legs, Feet, Body, Chest, Haar, Hände. Der Waffen-Slot bleibt ein schwebendes Icon: eine einzige Klinge `cftool_sword` für alle drei Waffengattungen, unterschieden nur durch Tint und Größe. Der Schild-Slot ist gar kein Sprite, sondern ein Emoji über `ctx.fillText`, das nur Schriftgröße und Glanzfarbe ändert. Accessories sind nicht geladen.
Zählt, weil die Abnahme "Jede Slot-Belegung ändert den Helden sichtbar" lautet und der Phasentitel "Held modular (sichtbare Ausrüstung)" heißt. Beide Verzichte sind in den Umsetzungsnotizen begründet (`grafik:334-344`, `:388-389`), die Abnahmeformulierung wurde aber nicht angepasst.
Kosten: für den Schild gibt das Pack nichts her, hier wäre nur eine ehrliche Umformulierung der Abnahme möglich. Für die Waffe wäre ein echtes Tools-Layer eine Offset-Tabelle je Animation, also spürbarer Aufwand.

**F17. Die Fledermaus läuft auf einem Ein-Zeilen-Sheet: kein Angriff, kein Treffer, kein Sterben.**
`index.html:619-621` (`CF_RIGS.bat`), verwendet von `index.html:2456`.
`Halloween/Bat.png` ist 96x16, also eine Zeile mit 6 Frames. Alle sieben Anim-Keys zeigen auf `[0,6]`.
Zählt, weil die G3-Abnahme für jeden der 21 Typen idle, laufen, angreifen, casten wo zutreffend und sterben verlangt. Der Plan hatte `Flying_Skull` vorgeschlagen, dessen Rig getrennte hurt- und death-Zeilen hat und im Projekt bereits für `ghost`/`shadowghost` läuft. Der Kompromiss steht im Code-Kommentar, taucht aber in der Kompromiss-Liste der G3-Notizen (dort: spider, mummy, stalfos, Bosse, Hexe) nicht auf. Kleinerer Fall gleicher Art: `slime` teilt hurt und death auf Zeile 3.
Kosten: ein `rig`-Feld in `MONDEF` umhängen, oder den Kompromiss sauber nachtragen.

**F18. Der G5-UI-Skin macht die runden Touch-Menüknöpfe wieder eckig.**
`index.html:6272-6277` gegen `index.html:200-204` und `:213-215`.
`bakeUiSkin` legt `border-image` auf jedes `.beltSlot`. Im Touch-Modus sind `#spellsBtn`, `#invBtn`, `#prioBtn`, `#kamExitBtn`, `#potionBtn`, `#spellBtn` und `#ultBtn` genau solche Elemente mit `border-radius:50%`. `border-image` ignoriert `border-radius`.
Zählt, weil die runden Touch-Knöpfe eigene Vorarbeit waren (`7e90910`) und die G5-Abnahme "UI-Skin auf Desktop und Touch geprüft" nennt. Die eigene Umsetzungsnotiz `grafik:871-873` nennt genau dieses Argument als Grund, warum die Desktop-Orbs kein `border-image` bekamen; für die runden `.beltSlot`-Knöpfe wurde es übersehen.
Kosten: die runden Knöpfe aus dem Selektor ausnehmen und ihnen wie den Orbs `cfui_round` als `background-image` geben. Dieselbe Zeile existiert bereits.

**F19. Gold-Doppelbuchung: der "Abzug" geht an die Bank, verloren geht nichts.**
`index.html:5814`, `:5825-5826`, `:5856`.
`gameplay:200` listet unter "Bleibt dauerhaft" nur "Gold zu einem Anteil (Startwert 50 Prozent)". Der Code berechnet `carryGold` als 50 Prozent und verbucht denselben Betrag zweimal: als Startguthaben der Folgeschicht und als `amt.bankGold`. In Summe überleben 100 Prozent, aufgeteilt auf zwei Töpfe.
Zählt aus drei Gründen. Die Berichtszeile "Beuteanteil nach Abzug" liest sich als Verlust. Die Ökonomie konvergiert gegen `W = E`, die Bank kassiert im eingeschwungenen Zustand pro Schicht volle Schichteinnahme statt der Hälfte, das verdoppelt das Ausbau-Tempo und verschärft F20. Und `player.gold` hat während der Schicht überhaupt keine Ausgabestelle, der Geldbeutel ist reines Zwischenlager.
Kosten: Entscheidung, kein Umbau. Entweder `amt.bankGold += player.gold - carryGold` (dann stimmt "nach Abzug"), oder die Doppelbuchung bleibt und der Bericht wird ehrlich beschriftet.

**F20. Die Ausbau-Kosten liegen etwa eine Größenordnung unter den Schichteinnahmen.**
`index.html:5742-5749`, `:5891`, `:5895`, gegen `index.html:3621`.
Vollausbau kostet 3850 Gold (1800 startLevel + 1650 kontingent + 240 tueren + 100 Vermutungen + 60 Startfluch). Eine Kammertruhe wirft im Erwartungswert 240 Gold. Im Grundausbau stehen 6 Türen gleichzeitig offen, Nachwachszeit 120s gegen 900s Schichtdauer. Sechs Truhen ohne jede Wiederholung ergeben schon 1440 Gold, dazu Monsterdrops.
Zählt, weil die erste Kaufrunde aller fünf Ausbauten zusammen 310 Gold kostet, also nach einer Schicht bezahlt ist, und der komplette Baum etwa ab Schicht 3 bis 4 ausgekauft ist. Das erste Jahresgespräch kommt aber erst bei Schicht 10. Dazwischen gibt es keine Goldsenke mehr, `amt.bankGold` wächst ins Leere, und der Dorf-Hub ist tot, bevor der erste Jahresbonus ausgeschüttet wird. Zusätzlich ist `tueren` mit 240 Gesamtkosten der mit Abstand stärkste Ausbau (er skaliert direkt die Truhenzahl) und kostet weniger als ein Siebtel von `startLevel`. Im Plan als Schätzung markiert (`gameplay:246`).
Kosten: reines Zahlen-Tuning, aber ohne durchgespielte Schicht nicht seriös festzulegen.

**F21. `MUS.duck(ms)` steht in der Schnittstellenliste des Plans, existiert aber nicht.**
`gameplay:509` gegen `index.html:1405-1460`.
Der Plan nennt `MUS` als "die einzige nach außen sichtbare Schnittstelle" und listet sieben Methoden. Das Objekt hat sechs. Die freie Funktion `duckToFx(ms, depth)` existiert, ist aber nicht auf `MUS` gehängt. Zur Laufzeit bestätigt: `Object.keys(MUS)` liefert `goto, layer, swell, sting, muffle, setOvertime`.
Zählt, weil der erste Aufrufer, der sich auf den Plan verlässt, einen TypeError bekommt, und zwar nicht beim Laden, sondern erst wenn die Spielsituation eintritt.
Kosten: eine Zeile Wrapper, oder die Planzeile korrigieren.

**F22. W1 und W2 sind vollständig gebaut, tragen aber in keinem Plandokument einen Statusmarker.**
`phase-w1-terminologie.md:1`, `phase-w2-aktenfunde.md:1`, `superduper-weltbibel.md:508` und `:522`.
Beide sind implementiert, committet und gepusht. Die Gameplay-Phasen bekamen jeweils ein `— ERLEDIGT` in den Kopf, zuletzt eigens nachgezogen in `1455571`. W1 und W2 nicht.
Zählt, weil wer nur die Plandateien liest, beide für offen hält und entweder ein zweites Mal baut oder W3 hinter einer angeblich noch offenen Vorstufe plant. Genau die Gegenrichtung des Fehlers, den `1455571` für Phase 5 repariert hat.
Kosten: zwei Kopfzeilen plus zwei Worte in der Weltbibel. Minuten.

**F23. Drei Planabschnitte beschreiben einen Zustand, den G5 überholt hat.**
`gameplay:244` behauptet, ein Dorf existiere im Code nicht und die einzige Landmarke sei der Kessel-Prop. `gameplay:540-542` erklärt `ZONES.village` für "von nirgends aufgerufen" und formuliert die Aufgabe für später. `gameplay:229` nennt für `endShift(reason)` nur `'tod'` und `'zeit'`.
Alle drei sind falsch. Verifiziert: `MUS.goto(bossFight && currentLevel === 2 ? 'boss' : inVillage ? 'village' : zoneForLevel(currentLevel))` an `index.html:4724`, also ist die Dorf-Zone erreichbar; `VILLAGE` an `:1497` mit sechs Gebäuden und drei NPCs; `endShift('amt')` an `:4063` als dritter Anlass.
Zählt, weil die Umsetzungsnotizen laut Plankopf ausdrücklich "für die Folgephasen wichtig" sind und W3 ("Das Dorf spricht") direkt auf der falschen Voraussetzung aufsetzen würde. `gameplay:540` liest sich zudem als offener Restposten, obwohl die Arbeit erledigt ist.
Kosten: drei Absätze umschreiben. Minuten.

**F24. Das Kammerschild sagt weiterhin KAMMER, nur Knöterichs Verweis darauf wurde umgestellt.**
`index.html:4249-4254` gegen `index.html:3036`, `:3120`, `:3172`.
Die Weltbibel führt für W1 den Punkt "Kammerschild heißt Gebührenbescheid" (`weltbibel:515`). Die W1-Plandatei streicht den Auftrag mit der Begründung, ein solches Schild gebe es im Code nicht ("nur die beiden Hinweistexte oben", `phase-w1-terminologie.md:135`). Die Prämisse ist falsch: `drawKammerTuer()` malt vor jeder Tür ein Schild mit Kopfzeile `'KAMMER'`, darunter Balkenanzeige und `RARITY`-Name. Verifiziert an `index.html:4254`.
Zählt, weil der Spieler zwei Namen für dasselbe Ding im selben Moment liest: Knöterich sagt "Gebührenbescheid lesen. Steht alles drauf.", und das Ding heißt KAMMER, Gewöhnlich bis Einzigartig. Hier ist der Plan die Fehlerquelle, nicht die Umsetzung.
Kosten: eine Zeichenkette, aber die Schildbreite ist mit 68px fest, ein längeres Wort braucht zwei Zeilen oder eine Abkürzung. Die Plandatei muss vorher korrigiert werden.

**F25. Der Spieltitel steht in zwei Schreibweisen, auf zwei unmittelbar aufeinanderfolgenden Bildschirmen.**
`index.html:6207` (`Das Monstralministerium`) gegen `index.html:5657` (`DAS MONSTRAL MINISTERIUM`), `index.html:6` (`<title>`).
`showLoading()` schreibt die zusammengeschriebene Form, `showStartScreen()` überschreibt dasselbe Element mit der getrennten. Verifiziert im Live-Lauf: Tab-Titel "Das Monstralministerium", Startbildschirm "DAS MONSTRAL MINISTERIUM".
Der Widerspruch steckt bereits in den Quellen: `phase-w1-terminologie.md:17` nennt den Startbildschirm als Ort der zusammengeschriebenen Form, `:36` schreibt dort die getrennte vor. `weltbibel:831` verlangt für jeden Spielstring die Zusammenschreibung, `:863` schreibt für den Startbildschirm die getrennte Fassung. Entstanden ist das Nebeneinander erst durch die Nachbesserung `1b47e8e`.
Kosten: eine Entscheidung, danach eine Zeile. Sauberer wäre, die getrennte Fassung als echtes zweizeiliges Logo zu setzen, wie `weltbibel:828-829` es vorgibt, dann ist der Unterschied als Gestaltung lesbar.

**F26. Der alte Bossname Abaddon steht weiter im Spiel, einmal als großer roter Einblender.**
`index.html:4800` und `index.html:344`.
W1 hat den Boss auf `name:'Schattenfürst'` plus `art:'Fürst Nachtrag'` umgestellt. Zwei Stellen tragen den alten Namen. Der Platzhalter im Bossleisten-Span ist unkritisch (`#bossbar` ist bis zum ersten Setzen `display:none`, unabhängig nachgeprüft: der einzige Pfad ohne Namenssetzung ist `verlasseKammer()`, dort ist `boss` immer `null`). Der Floater `'ABADDON ERWACHT!'` ist es nicht: rot, groß, 5 Sekunden.
Zählt, weil das der prominenteste Moment des Spiels ist und daneben die Bossleiste "Schattenfürst · Fürst Nachtrag" zeigt. Alt und neu gleichzeitig auf demselben Bildschirm. Keine Datenbindung hält den Namen.
Kosten: zwei Zeichenketten, kein Zeichendeckel betroffen.

**F27. Der Siegesbildschirm ist vollständig im vor-W1-Register geblieben.**
`index.html:5697-5702`, dazu `:2519`, `:2922`, `:4800`.
`winGame()` besteht aus "HORDE BESIEGT!", "Eine wahre Schnetzel-Legende." und "NOCHMAL METZELN". Der W1-Plan hält fest, der ersetzte Startbildschirm-Untertitel sei "der letzte Text im Spiel, der zwinkert". Das stimmt nicht.
Zählt, weil hier kein einzelnes Wort, sondern ein kompletter Bildschirm im alten Ton neben einem Startbildschirm im neuen steht, und weil es die Selbstauskunft des W1-Plans direkt widerlegt.
Kosten: vier bis fünf Zeichenketten, aber es ist Texterarbeit im Amtsdeutsch-Register und gehört inhaltlich zu W3.

**F28. Vier Gedankenstriche in dauerhaft sichtbaren HUD-Zeichenketten.**
`index.html:351`, `:5632`, `:5635`, `:5641`.
Der vollständige Unicode-Scan findet 98 Vorkommen von U+2013/U+2014 in der Datei; 94 sind Kommentare. Vier stehen in sichtbaren Zeichenketten: der Platzhalter des Zauber-Gürtelfachs im HTML (`'— (E)'`), derselbe Platzhalter beim HUD-Update, und die Tooltips von Zauber- und Ultimate-Knopf.
Zählt, weil `weltbibel:499` und Kapitel 19 "Keine Gedankenstriche in Spieltexten" zur verbindlichen Formregel machen und die W1-Plandatei sich mehrfach ausdrücklich darauf beruft. Der Platzhalter steht bei jedem neuen Spieler von der ersten Sekunde an im Gürtel. Bestand aus früheren Phasen, weder von W1 noch von W2 eingeführt. Zusatzbefund: in JS-String-Literalen selbst wurden per vollständigem Perl-Scan **null** Gedankenstriche gefunden, die vier stecken in HTML-Markup und Template-Literalen.
Kosten: vier Zeichen. `index.html:351` und `:5635` müssen dieselbe Ersatzform bekommen, sonst springt der Platzhalter beim ersten `updateHUD()`.

**F29. `updateHUD` schreibt zwei `title`-Attribute pro Frame ohne Dirty-Check.**
`index.html:5632`, `:5641`, gegen `:5631`.
Alle Textwerte gehen korrekt durch `setTxt`/`setHTML`/`setStyle`, die beiden Tooltip-Titel werden direkt am Element gesetzt, jeden Frame, jeweils aus einem frisch gebauten Template-String. Direkt darüber macht dieselbe Funktion es für den sichtbaren Text richtig.
Zählt, weil Regressionsregel 1 "HUD schreibt nur bei echter Wertänderung" lautet, nicht "HUD schreibt Texte nur bei echter Wertänderung". Es ist der einzige ungeschützte HUD-Schreibpfad, den der Code noch hat, und er steht direkt neben der korrekten Lösung.
Kosten: minimal. Ein `setAttr(id, name, v)` nach dem Muster von `setStyle`, oder die Titel dort setzen, wo sich der aktive Zauber ändert.

**F30. `tintedSheet()` baut pro getöntem Sprite pro Frame einen Cache-Key-String.**
`index.html:808`, Aufrufer `:829`, `:5528`, `:5561-5562`.
Der Canvas selbst ist gecacht und wird beim Boot vorgewärmt. Der Schlüsselstring wird bei jedem Zeichenaufruf neu erzeugt. 18 `MONDEF`-Einträge tragen ein festes `tint`, dazu tönt jeder Trefferblitz und jede Leiche.
Zählt, weil Regressionsregel 4 neue Allokationen im Renderpfad ausdrücklich verbietet und das die einzige gefundene Allokation ist, die mit der Hordengröße skaliert: bei 60 sichtbaren getönten Monstern 60 Stringallokationen pro Frame. Genau der Lastfall, für den das 0,6-ms-Budget aus `grafik:79` gilt.
Kosten: klein. Cache-Key einmalig pro Kombination vorberechnen, oder `TINT_CACHE` zweistufig als Map führen.

**F31. Dungeon-Sheet-Keys werden pro Frame per Template-Literal zusammengebaut.**
`index.html:4143`, `:4153`, `:4209`, `:4212`, `:4244`, `:5497`.
`drawKammerObj()`, `drawKammerTuer()` und `drawProp()` erzeugen ihre Keys jedes Frame neu aus `kammer.set` beziehungsweise `t.diff`, statt sie einmal beim Kammeraufbau abzulegen. Betrifft Tor, Bodenplatte, Treppe, Kammertür und Säule.
Zählt, weil es nachweislich während der Phasenarbeit entstanden ist, nicht Altbestand: `git log -S 'dun${kammer.set+1}_pillar'` zeigt `046afce` (G1), also nach Abfassung des Regressionsblocks. Der Regressionsschutz hat hier nicht gegriffen.
Kosten: klein und lokal. Ein Prefix beim Kammeraufbau berechnen. Sechs Zeilen plus eine Zuweisung.

**F32. Zwei Closures werden pro Frame neu allokiert.**
`index.html:5197` (`const vis = (x,y)=> …`), `index.html:5628` (`SPELLS.find(s=>s.id===activeSpellId)`).
Regressionsregel 4 sagt wörtlich "Keine Closures pro Frame". Der Pool-Teil derselben Regel ist mustergültig umgesetzt (`drawPool`, Komparator `byY` hochgezogen), der Closure-Teil nicht. Beide Stellen sind Altbestand, also keine neue Regression, aber die Regel behauptet einen Zustand, den der Code nicht hat. `activeSpellId` ändert sich nur bei Nutzeraktion, wird aber pro Frame linear gesucht.
Kosten: sehr klein. `vis` auf Modulebene, aktiven Zauber einmal bei `setActiveSpell` in eine Variable schreiben.

**F33. Das G5-Amtsfenster zeigt die Schichtnummer um eins zu niedrig.**
`index.html:4056` gegen `:3234`, `:5826`, `:5928`.
`amt.schichten` zählt abgeschlossene Schichten und wird erst in `endShift()` hochgezählt. Phase 4 formuliert konsequent in der Vergangenheit ("Schicht N abgeschlossen"), Phase 5 rechnet für die laufende Schicht ausdrücklich um (`const n = amt.schichten + 1`). Das G5-Panel druckt den Rohwert während der laufenden Schicht. Verifiziert.
Zählt, weil in der ersten Schicht "Schicht 0" im Amt steht, während Knöterich daneben "Zweite Schicht. Die Akte wird dick." sagt. Die richtige Umrechnung steht bereits zweimal im Code.
Kosten: ein Plus eins.

**F34. Der G5-UI-Skin skaliert 16- und 48-px-Pixelkunst ohne `pixelated`-Rendering.**
`index.html:6273-6285` gegen `:9` und `:31`.
`background-image` aus `round_brown.png` (16x16) mit `background-size:100% 100%` auf `.orbWrap`, `.tBtn` und `#attackBtn` (78x78), also 3,5- bis 5-fache Hochskalierung. `image-rendering:pixelated` steht im gesamten Stylesheet nur an zwei Stellen: `#game` und `#minimap`.
Zählt, weil Regressionsregel 14 (`grafik:77`) verlangt, das pixelated-Rendering konsistent zu halten, "damit 16/32-px-Kunst scharf bleibt", und CSS als gleichberechtigten Weg nennt. Der UI-Skin ist der einzige Ort, an dem Pixelkunst per CSS hochskaliert wird, und genau dort fehlt die Regel.
Kosten: sehr klein, `image-rendering:pixelated` in die betroffenen Selektoren. Ob es heute sichtbar weich aussieht, ist laufzeitgebunden und wurde nicht geprüft.

**F35. Zonenwechsel wartet bis zu vier Takte, nicht bis zur nächsten Taktgrenze.**
`index.html:1353` gegen `gameplay:508`.
Das Gate prüft `stepIdx % z._totalSteps`, und `_totalSteps` ist `_stepsPerBar * bars`, also die komplette Viertaktphrase; alle sechs Zonen haben `bars:4`. Ausgerechnet: overworld bis 8,6s, shadowland 9,6s, chamber 12,6s, village 7,8s, office 4,6s, boss 6,9s. In der Überstunde bis zu 14 Prozent obendrauf.
Zählt, weil das Abnahmekriterium "ohne Schnitt mitten im Takt" zwar erfüllt ist, die Beschreibung der Mechanik aber nicht. Praktisch: nach dem Verlassen einer Kammer läuft die Kammermusik bis zu 12,6s weiter. Am stärksten trifft es die neue `village`-Zone, weil man deren Grenze im Gehen überschreitet.
Kosten: eine Zeile, wenn die Mechanik angeglichen werden soll. Billiger und ehrlicher: die Planzeile auf "Phrasengrenze (vier Takte)" korrigieren.

**F36. "Alle Panels dämpfen die Musik" gilt nur für die Overlay-Screens.**
`index.html:4334`, `:2813`, `:4643`, `:4069/4074`, `:4045/4050`.
Umgesetzt ist die Dämpfung nur für die `#ovPanel`-Stellen. Inventar, Zauberbaum, Kessel, Schloss-Rätsel und Amtsfenster rufen kein `MUS.muffle`.
Zählt, weil genau diese fünf im Projekt als Panel gelten: `knSperrzone()` fasst sie als "offenes Panel"-Sperrzone zusammen, und `MUS.sting` begründet seinen `muffled`-Guard wörtlich mit "während ein Panel offen ist". Dieser Guard greift bei ihnen also nie, der Knöterich-Stinger kann mitten in ein offenes Kesselfenster hineinspielen. Der Planfließtext `:546` engt selbst auf `#ovPanel` ein, die Abnahme `:556` tut das nicht; der Widerspruch steht innerhalb derselben Phase.
Kosten: überschaubar, aber an fünf Stellen, und es braucht einen gemeinsamen Zähler, sonst hebt das Schließen eines Panels den Muffle auf, während ein zweites noch offen ist.

**F37. Die Dorf-Gebäude überragen die Cull-Ränder.**
`index.html:5195-5196`, `:5206-5209`, Ankerdaten `:710-715`.
Die Sichtbarkeitsgrenzen stammen aus der Zeit vor den Gebäuden: 80px Rand zur Seite und nach unten. `cfbld_amt` hat `ay:190` und `ax:120`, die Häuser `ay:126`, die Scheune `ay:142`.
Zählt, weil ein Gebäude verworfen wird, sobald sein Fußpunkt mehr als 80px unter den Bildrand rutscht, obwohl sein Dach noch im Bild steht. Beim Amt bis zu 110px Dach, die in einem Frame verschwinden statt herauszuscrollen. Trifft den Standardweg vom Frostkamm zurück ins Dorf. Vor G5 gab es das Muster nur bei der Windmühle mit 30px.
Kosten: `cullB` auf 200 und `cullL`/`cullR` auf 130 anheben, oder ein eigener Rand für `big:true`-Decos.

**F38. Gebäude-Fußabdrücke ragen als andersfarbige Rechtecke unter den Häusern hervor.**
`index.html:1734-1740` (G_WALL-Footprints), `:1649-1672` (kein G_WALL-Zweig im Oberwelt-Bake).
Die Footprints werden auf `G_WALL` gesetzt; `initFloorGraphics` hat im Oberwelt-Zweig keinen `G_WALL`-Fall und backt sie als Gras, im Schattenland als Pilzland-Lila, mitten in der hellbraunen Pfadfläche. Der Codekommentar behauptet, das Gebäude-Sprite decke die Fläche ab. Der Prüfagent hat die opake Sprite-Boundingbox gegen den Footprint gemessen: 5 von 6 Gebäuden lassen sichtbare Ränder frei, das Amt 18px links, 8px rechts, 13px unten.
Kosten: entweder einen `G_WALL`-Zweig ergänzen, der im Dorf die Pfadkachel backt, oder die Footprints an die tatsächliche Sprite-Breite anpassen.

**F39. Der gebackene Boden ist ein regelmäßiges Schachbrett, nicht gestreut.**
`index.html:1588-1591` (`pickCfTile`), `:1647` (`tileHash`).
Die Variante wird über `(tileHash(x,y) >>> 4) % list.length` gewählt. Bei ungeraden Multiplikatoren hängen die Bits 4/5 nur von den niedrigen Bits von x und y ab. Der Prüfagent hat es im echten Ladelauf gemessen: Anteil gleicher Nachbarn in x und y jeweils 0,0 Prozent, Variantenzeilen 13-mal hintereinander abwechselnd identisch.
Zählt, weil die G4-Abnahme "keine sichtbaren Kachelfugen" fordert und der Prompt "sparsam gestreut (kein Schachbrett)". Die Umsetzungsnotiz `grafik:582-589` behandelt genau dieses Problem, benennt aber die falsche Ursache, und der Codekommentar ("Bits >>>4 verwendet, nicht die rohen Low-Bits") erreicht das Ziel nicht: auch Bit 4/5 sind low bits.
Kosten: eine fmix-/xorshift-Runde auf `tileHash`, danach am laufenden Spiel gegensehen.

**F40. Zwei Wege ins Amt mit unterschiedlichem Funktionsumfang.**
`index.html:4008`, `:4045-4064` gegen `:5903-5935`, Auftrag `grafik:165-166`.
Der Plan sagt, das Amt-Overlay `#ovPanel` bleibe funktional wie es ist und das Gebäude sei der Ort, an dem man es öffnet. Gebaut wurde ein zweites, eigenes Panel `#amtFenster` mit reiner Anzeige, ohne Preise und ohne Kaufknopf, mit dem gegenläufigen Ausgang "FEIERABEND NEHMEN" statt "NÄCHSTE SCHICHT ANTRETEN", und anderer Überschrift für dasselbe Haus.
Zählt, weil die Abnahme "Amt über Gebäude erreichbar" lautet, erreichbar aber nur ein Amtsfenster ist, in dem man nichts kaufen kann. Die Abweichung ist als Nutzerentscheidung dokumentiert (`grafik:722-725`, `:864-866`), also bewusst, und die Begründung trägt: `showDorf()` setzt `state='feierabend'` und hätte die laufende Schicht hart zurückgesetzt. Der Auftragswortlaut war technisch nicht erfüllbar.
Kosten: nichts zu reparieren, solange die Entscheidung gilt. Billigste Angleichung: dieselben Preise anzeigen und die Knöpfe deaktiviert mit Begründung zeigen statt weglassen.

**F41. `Math.hypot` läuft an sechs Stellen im Frame-Pfad.**
`index.html:4845`, `:4855`, `:5063`, `:5070`, `:5071`, `:5313`.
Regressionsregel 3 verbietet `Math.hypot` in Hot Paths, die Ersatzhelfer stehen samt Begründung im Code. Die drei NPC-Fundstellen sind neu: `git log -S 'Math.hypot(n.x-n.homeX'` zeigt `9909c91` (G5), also nach Abfassung beider Regressionsblöcke.
Der Aufwand ist heute klein (`NPC_DEFS` hat drei Einträge, also höchstens sechs Aufrufe pro Frame). Es zählt, weil G5 die Regel bei neuem Code nicht angewendet hat: `:5070/5071` vergleichen nur gegen `NPC_HOME_R` und könnten wortwörtlich das vorhandene `sqDist` gegen `NPC_HOME_R*NPC_HOME_R` nutzen, die Wurzel wird dort gar nicht gebraucht.
Kosten: sehr klein, sechs Ausdrücke.

**F42. Abnahmepunkt G1 verweist auf eine Zusicherungs-Suite, die es nicht gibt.**
`grafik:113`.
Die G1-Abnahme verlangt, dass "die bestehende Zusicherungs-Suite aus Phase 2 grün bleibt". Im Repo existiert keine solche Suite. Die einzigen Assertions sind `knAssertCaps()` (Phase 5) und `assertRigRegistrations()` (G3), beide ohne Bezug zu Kammern. Der Begriff kommt im Gameplay-Prompt nirgends vor, die Phase-2-Abnahme dort beschreibt reine Handprüfungen.
Zählt, weil dieser Abnahmepunkt prinzipiell nicht erfüllbar und nicht widerlegbar ist und verdeckt, dass die Kammermechanik nach dem Grafikumbau nur von Hand und nur einmal geprüft wurde.
Kosten: Punkt streichen oder auf "Handdurchlauf aller 8 Module" umformulieren, oder eine echte Boot-Assertion für die Kammer bauen.

**F43. G0-Punkt 6 ist nur als Cast-ja/nein erfüllt, das Manifest hat gar keine Anim-Zuordnung.**
`assets/cf/manifest.json`, Feld `anims`.
885 von 886 Einträgen haben `anims: null`. Geliefert wurde stattdessen eine Cast-ja/nein-Tabelle für 30 Prioritätsrigs, davon 8 nach eigener Angabe nicht selbst am Bild geprüft.
Zählt, weil der Plan genau diesen Punkt als Lehre aus dem Sunnyside-Umzug hervorhebt und sich die Lücke in G2 direkt gerächt hat: dass dem Player-Rig Angriff, Zauber, Treffer und Tod komplett fehlen, fiel erst beim Einbau auf. Der Verzicht ist in `grafik:213` begründet, bleibt aber eine offene Aufgabe des Fundaments.
Kosten: klein, wenn auf die tatsächlich verbauten Rigs beschränkt; die stehen ohnehin als `_rigTable` mit 25 Einträgen in `tools/sheet-audit.overrides.json`.

**F44. `.gitignore` und Plan sagen über den Pages-Build Widersprüchliches.**
`.gitignore:20-21` gegen `docs/index.html`; `grafik:874-877` gegen `:899`; `tools/build-single.mjs:34`.
Drei Teilbefunde. Erstens begründet `.gitignore:20` den Ausschluss von `dist/` mit "Build-Artefakt, enthält die eingebackene Grafik", während die byte-identische Datei als `docs/index.html` getrackt ist. Zweitens führt `grafik:874-877` die Pages-Umstellung weiterhin unter "Bewusst nicht automatisch gemacht … dafür erst das ausdrückliche Go einholen", während `:899` 25 Zeilen weiter "Live auf GitHub Pages verifiziert" meldet. Drittens schreibt der Build per Default nach `dist/index.html`; ein `docs/`-Pfad kommt im Skript nirgends vor, und in keiner Plandatei steht, dass `docs/index.html` nach einer Änderung neu gebaut werden muss.
Zählt, weil ein vergessener Kopierschritt die öffentliche Seite still veralten lässt, ohne dass `git status` oder ein Check anschlägt. Heute in Ordnung: ich habe nachgerechnet, `docs/index.html` ist ein exakter Build der aktuellen `index.html` (identische Zeilenzahl, einziger Unterschied ist die `ASSET_BLOBS`-Zeile), und GitHub Pages liefert byte-identisch dieselbe Datei aus. Dass `1b47e8e` beide Dateien im selben Commit mit je einer Zeile korrigieren musste, zeigt aber, dass der Schritt Handarbeit ist.
Kosten: zwei Zeilen Kommentar, ein Satz im Regressionsschutz, optional ein npm-Script. Etwa 20 Minuten.

### Kosmetisch

**F45.** `stats.goldTotal` ist deklariert (`index.html:2278`) und wird in der gesamten Datei kein weiteres Mal erwähnt. Zur Laufzeit bestätigt: nach 10 gespielten Schichten steht der Wert auf 0. Kein Spielschaden, aber eine gestellte Falle genau dort, wo jemand sie ahnungslos einbauen würde. Kosten: zwei Zeilen ergänzen oder ersatzlos streichen.

**F46.** `shiftElapsedT` (`index.html:2275`, `:4702`, `:5796`) wird deklariert, zurückgesetzt und jeden Frame hochgezählt, aber nirgends gelesen. Anders als `goldTotal` nicht einmal im Plan benannt. Kosten: eine Zeile.

**F47.** `stats.kills` wird nie zurückgesetzt und nie gespeichert, summiert also über alle Schichten einer Browser-Sitzung und ist nach jedem Reload 0. Dasselbe Muster wie F1. Kosten: eine Zeile, je nach Absicht Reset in `startShift()` oder Verlagerung nach `amt`.

**F48.** `MUS.swell()` (`index.html:1420-1431`) ist vollständig implementiert und hat keinen einzigen Aufrufer. Der Plan listet es in der Schnittstelle, die Umsetzungsnotiz führt es unter den getesteten Dingen auf, ohne je zu sagen, wo es hängen soll. Nebenwirkung: `swellGain` hängt ebenfalls an der Stummschaltung vorbei (F3). Kosten: anschließen oder streichen.

**F49.** Der Zonenwechsel duckt und startet die Phrase neu, auch wenn sich die Zone gar nicht ändert (`index.html:1353-1357`, `gotoZone` vergleicht nicht gegen `currentZoneKey`). Ausgelöst durch normales Spielverhalten, weil die Dorfgrenze eine Rechteckkante mitten in der begehbaren Oberwelt ist. Kosten: eine Bedingung erweitern.

**F50.** `initAudio()` trägt einen bereits gesetzten Muffle-Zustand nicht in den Lowpass ein (`index.html:986` gegen `:1013-1014`). Solange das Startbild steht, ist der Muffle zu zwei Dritteln aktiv: Hall und Pegel ja, Tiefpass nein. Genau das Fenster, in dem der Spieler die Musik zum ersten Mal hört. Kosten: eine Zeile.

**F51.** `shadowland.scale` ist äolisch, obwohl der Plan die Zone als phrygisch führt (`index.html:1213` gegen `:1204`). Die Melodie hat korrekt Bb, die Skalentabelle nicht; sie ist zeichengleich mit `chamber` und `boss`. Derzeit stumm, weil keine der drei Stinger-Varianten die Stufe 1 anfasst, aber scharf: eine vierte Variante spielt im Schattenland ein H gegen ein B. Kosten: ein Zeichen, danach gegenhören.

**F52.** `item.fluchRuht` bleibt an abgelegten Stücken stehen (`index.html:2318` nur im Equip-Zweig, `:4354-4364` ohne Reset), der Tooltip behauptet dann fälschlich einen ruhenden Fluch. Mechanisch harmlos, weil `CFX` in `recalc()` vollständig genullt wird, aber eine Falschaussage genau dort, wo Phase 3 auf Ehrlichkeit setzt. Kosten: eine Zeile.

**F53.** Kammer-Abbruch über die Kontextaktion "Verlassen" zählt nicht als Abbruch (`index.html:4030` ruft `verlasseKammer()` direkt). `knAbbruchKammer` hängt an genau den zwei Pfaden, die `gameplay:382` nennt, also buchstabengetreu; der Effekt widerspricht aber der Absicht, weil der Rückweg zum Ausgang der Normalfall ist. `kn.counters.kammerAbbrueche` zählt systematisch zu niedrig. Kosten: trivial, aber vorher mit dem Plan klären.

**F54.** `knRandnotiz()` zieht den Textpool vor dem Zeitvergleich (`index.html:3226-3231`), obwohl `gameplay:376` die umgekehrte Reihenfolge ausdrücklich als Pflicht formuliert. Bis zu neunmal `Math.random` plus Arrayzugriff pro Aufruf, obwohl das 40-Sekunden-Gate die Zeile meist sofort verwirft. Hot Path ist `hurtMon`. Kosten: Gates in eine eigene Funktion ziehen.

**F55.** `knAssertCaps()` dupliziert Beat- und Steckenbleib-Literale, statt sie aus einer Tabelle zu lesen (`index.html:3111-3130`). `HINWEISE`, `ESCALATE_DEFS`, `RANDNOTIZ` und `KN_TRAENKE_GAGS` werden dynamisch gelesen, dort kann nichts durchrutschen; die Beats, die drei Steckenbleib-Texte und die Schichtbegrüßung stehen als handkopierte Zweitfassung im Rumpf. Alle sechs Paare stimmen heute zeichengenau überein, W1 hat "Schild lesen. F." brav an beiden Stellen nachgezogen. Nicht abgedeckt sind der Jahresgespräch-Satz (`index.html:5875`) und das Rekord-Suffix aus `knBegruessungLine` (`:3237`). W2 und G5 haben keine neuen Knöterich-Sprechtexte hinzugefügt, per `git log -S` bestätigt. Zur Laufzeit selbst geprüft: `knAssertCaps()` liefert `true`, keine Konsolenmeldung. Kosten: klein, Tabelle statt Kopie.

**F56.** Der Regler stummt zusätzlich den Dienstzettel-Sting (`index.html:3177`), obwohl `gameplay:411` sagt "Dienstzettel laufen in jeder Stellung". Praktisch sinnvoll, aber nirgends dokumentiert. Kosten: eine Zeile Notiz.

**F57.** Knöterichs Kopf ist ein Unicode-Emoji statt der geplanten Sprite-Briefmarke, und der Stempel fehlt in beiden Kanälen (`index.html:441-442`). Zettel und Randnotiz unterscheiden sich damit optisch nur über `font-style:italic`. Kosten: klein mit Textstempel, mittel mit echter Sprite-Briefmarke.

**F58.** Knöterich stellt sich mit "Knöterich. Amt für Monsterangelegenheiten." vor (`index.html:3279`, `:3299`, Prüflisten-Kopie `:3115`). Die Namenstabelle in `weltbibel:838-841` kennt genau drei Formen, diese ist keine davon. W1 schützt jeden bestehenden Amt-String, also kein W1-Verstoß. Achtung beim Ändern: "Knöterich. Ministerium für Monsterangelegenheiten." ergibt 49 Zeichen und reißt den z1-Deckel von 48 um genau eines. Gehört in W3.

**F59.** Zwei sichtbare Zeichenketten mit Blut- und Sterbe-Vokabular: `blutmagie` mit `kurz:'Blutmagie'` (`index.html:2021`) und `zerplatzer` mit `satz:'Sterbende Gegner zerplatzen dir ins Gesicht.'` (`:2036`). Beide Felder erscheinen in Kladde, Startfluch-Knöpfen und Tooltip. Grundgesetz 8 lautet "Kein Blut, kein Sterben". Die Darstellung selbst hält die Regel sauber ein (der Renderer malt ausdrücklich Konfetti statt Blut, Knöterich sagt konsequent "Sie verlieren Konfetti"). Nur diese zwei Strings sind Vorbestand aus Phase 3. Wichtig beim Ändern: nur `kurz` und `satz` anfassen, der interne Schlüssel `blutmagie` und `cfx:'blut'` hängen an `CFX.blut`.

**F60.** Totenkopf-Emoji und senkrechter Strich im Schattenland-HUD (`index.html:4778`), während alle anderen HUD-Zeilen den Interpunkt verwenden. Kosten: eine Zeichenkette.

**F61.** Der von G5 eingefügte vierte Zonen-HUD-Zweig folgt der W1-Konvention nicht (`index.html:4792`): "Dorf" ohne amtliche Bezeichnung, direkt neben "Frostkamm, Die Eisablage" und "Grasland, Ablage A". Die Weltbibel hält "Vordermühl an der Ablage" bereit, das Muster wäre ohne Erfindung bedienbar. Kosten: eine Zeichenkette.

**F62.** "Level" und "Stufe" stehen nebeneinander, an einer Stelle im selben Satz: `AUSBAU_DEFS` sagt `desc:'Startet jede Schicht ein Level höher.'` mit `unit:l=>'Stufe '+(1+l)` (`index.html:5744`), gerendert als "Startet jede Schicht ein Level höher. Jetzt: Stufe 1". Zweiter Rest: der Ladezustand von `#zone` (`index.html:342`), den W1 im eigenen Diff angefasst und dabei "Level 1" stehen gelassen hat. Kosten: zwei Wörter. Der Schlüssel `startLevel` ist Persistenz und bleibt.

**F63.** Der Kommentar am Level-1-Zeichenblock beschreibt seit G5 das Falsche (`index.html:5210`: "Hoftiere, Kessel und Kammertüren gibt es nur im Wald"), obwohl G5 in denselben Block die Dorf-NPCs eingesetzt hat und `KESSEL_T` und `KN_T` beide im Dorfrechteck liegen. Er zählt drei Dinge auf, der Block enthält fünf. Kosten: ein Kommentar.

**F64.** Das Dorf steht im Schattenland unverändert in Tagfarben (`index.html:5234-5238`, `:5212`). Der Schattenland-Override greift nur für nicht-`big`-Decos, die sechs Gebäude sind `big:true`; die drei NPCs werden ohne Level-Prüfung gezeichnet und wandern weiter. Knöterich wird korrekt ausgeblendet, die Gebäude nicht. Zusammen mit F8 ergibt das ein volles Dorf in Tagesoptik samt Bewohnern mitten in der Horde. Der Plan sagt zum Schattenland-Dorf nichts, also keine Planverletzung. Kosten: je eine Bedingung.

**F65.** Der Kommentar über `KAM_WAECHTER` nennt sieben Typen, das Array hat sechs (`index.html:3376-3377`). Der siebte, `bossgeneric`, ist an zwei anderen Stellen verdrahtet. Inhaltlich erfüllt die Abnahme, aber wer nachzählt, kommt auf sechs. Kosten: eine Zeile.

**F66.** `AFFIXES.fmt` ist seit der Tooltip-Umstellung toter Code (`index.html:1839-1843`). Jeder Eintrag trägt weiterhin einen Zahlformatierer, obwohl Phase 3 den Tooltip auf `satz` umgestellt hat. Genau das, was Phase 3 aus dem UI entfernen sollte. Kosten: drei Zeilen.

**F67.** Lokale Konstante `kn` in `hurtMon` (`index.html:2621`) beschattet das globale Knöterich-Objekt `kn` (`:2999`). Heute harmlos, weil die einzige Knöterich-Nutzung im selben Block ein Funktionsaufruf ist. `hurtMon` ist aber die Funktion, die Phase 2 und Phase 3 beide angefasst haben und die als nächstes wieder angefasst wird. Kosten: umbenennen.

**F68.** Die `walk`-Animation des Helden wird gebacken, aber nie gezeigt (`index.html:524` gegen `:4751-4756`). Der Zustandsautomat kennt `death`, `attack`, `cast`, `hurt`, `run`, `idle`; `walk` kommt dort nicht vor. Vier von 36 gebackenen Frames und 23 `SHEET_LIST`-Einträge sind tot. `death` selbst ist korrekt aufgelöst (`anims.death = anims.hurt`, `index.html:899`). Kosten: winzig, streichen oder kommentieren.

**F69.** G1: Druckplatten nutzen `Dungeon_1_Pressure_Plate.png` statt der im Plan namentlich genannten `_Anim`-Datei. Begründet in `grafik:243` (die statische Datei hat genau die zwei gebrauchten Zustände). Wer die Abnahme wörtlich abgleicht, findet die genannte Datei nirgends im Ladeweg. Keine Reparatur nötig, nur die Planzeile markieren.

**F70.** G1: Set-Mapping weicht ab (Stufe 3 bis 5 auf Set 1 statt Dungeon_3), weil `Graphics/Cute_Fantasy_Dungeons/Dungeon_3/` real leer ist. Dokumentiert und von der Plan-Klausel gedeckt. Gehört an die Planzeile selbst, nicht nur in die Notizen weit unten.

**F71.** `CREDITS.md` nennt zwei Packs, aus denen keine einzige Datei verwendet wird (Christmass, MilitaryCamp), und führt in Zeile 3 einen dritten Projektnamen ("Ministerium für Monsterangelegenheiten"), den weder der Titel noch die Fiktion im Code kennt. Zusätzlich nur zwei itch.io-URLs für sieben genannte Packs. Rechtlich ist Überattribution unschädlich, aber die Datei soll abbilden, was im Spiel steckt. Kosten: zwei Wörter streichen, eine Zeile korrigieren, optional URLs ergänzen.

**F72.** `bakeMinimap` schaltet Glättung bewusst ein (`index.html:5583`), alle übrigen Kontexte stehen auf `false`. Sachlich richtig, ein 20:1-Downscale mit Nearest-Neighbour würde die Karte in Rauschen auflösen, und der Anzeigepfad bleibt pixelig. Aufgeführt, damit die Ausnahme als Ausnahme geführt wird. Kosten: ein Halbsatz Kommentar.

**F73.** Weitere Allokationen im Renderpfad, alle konstant wenige pro Frame: `setLineDash`-Arrayliterale (`index.html:4122`, `:5265`, `:5272`), ein neuer `CanvasGradient` pro Frame für den Schwertbogen (`:5478`), Font- und rgba-Template-Strings (`:5449`, `:5470`, `:5482`), ein Arrayliteral im Dagger-Zweig (`:5465`), und `setStyle` selbst konkateniert pro Aufruf einen Cache-Key (`:912`). Verstöße gegen denselben Halbsatz von Regel 4, aber ohne Skalierung mit der Monsterzahl. Kosten: viele kleine Stellen.

**F74.** Der Zutaten-Tooltip enthält eine Zahl ("`${z.count}` im Beutel", `index.html:4303`), der Item-Tooltip ist sauber zahlfrei. Die Abnahme Phase 3 spricht ausdrücklich vom Item-Tooltip, also kein Verstoß. Aufgeführt, damit ein späterer Abgleich die Zahl nicht als Regression missdeutet: Bestandsmengen bleiben Zahlen, Ausrüstungswerte nicht.

**F75.** Der Dorf-Ausbau "Vermutungen" gibt die Kesselgrammatik sichtbar preis (`index.html:4563-4570`): für jedes nie gekochte Adjektiv die tatsächliche Zuordnung samt Fluch, direkt aus `ZUTAT_ADJ`. Wörtlich das, was `gameplay:284` verbietet, aber gedeckt durch `gameplay:212` als bezahlter Phase-4-Ausbau, und im Code offen benannt. Alle anderen Kanäle sind sauber: die 18 Blätter, sämtliche Knöterich-Zeilen, jeder Randnotiz-Eintrag. Nennenswert nur, dass das Wort "vermutlich" die einzige Absicherung ist; die Ausgabe ist immer korrekt. Keine Reparatur empfohlen.

**F76.** Weltbibel verbietet Minimap-Marker, die seit Phase 3 im Code stehen (`weltbibel:562` gegen `index.html:5598-5603`). Die Wirkung "Aktenlage" zeichnet ungeöffnete Truhen und bereitstehende Türen als gelbe Punkte. Kein Pfeil und kein Questmarker im engeren Sinn, aber ein Fundort-Marker. Die Weltbibel ist jünger als Phase 3 und nimmt den Bestand nicht aus. Kosten: eine Klammer in der Weltbibel.

**F77.** Der Kommentar an `CONFIG.zutatenMitnahmeBasis` (`index.html:2967`) spricht von Stapeln, die Kappung in `endShift()` zählt Einzelstücke und teilt Stapel bewusst auf. Die Anzeige im Amt sagt korrekt "Zutaten". Bei 5 als Basis ist das ein Faktor-Unterschied für die Mitnahme-Ökonomie. Kosten: ein Wort.

**F78.** Drei unvereinbare Statusmarker-Konventionen in vier Plandokumenten: Gameplay im `##`-Kopf, Grafik nur in der `###`-Notizüberschrift plus einer Tabellenzelle, Welt gar nicht. Es gibt keinen Ort, an dem der Gesamtstand steht; die einzige verlässliche Statusquelle ist `git log`, und die kennt kein Vorhaben, das noch nicht angefangen wurde. Kosten: eine Konvention wählen und 13 Kopfzeilen angleichen, oder eine Statustabelle anlegen. Unter einer Stunde.

**F79.** Regel "ein Commit pro Phase" systematisch abgewichen: G5 mit 4, G1 mit 3, W1 und Gameplay-Phase 5 mit je 2 Commits; auch bei den älteren Phasen 2, 3 und 4 je 2. Der Grund ist in jedem Einzelfall im Commit-Text erkennbar und legitim: `07492a3` und `d7a7c9f` warten auf eine Bestätigung, die erst später kam; `1b47e8e` repariert einen erst auf der Live-Seite aufgefallenen Fehler und zitiert den Vorgängercommit namentlich; `1455571` zieht einen vergessenen Statusmarker nach. Keine Schlamperei, sondern eine Regel ohne Platz für nachträglich eintreffende Information. Bemerkenswert nur, wie regelmäßig die Lücke auftritt: die Umsetzungsnotizen sind fast nie im Phasencommit. Kosten: ein Halbsatz im Regeltext.

**F80.** Getracktes 1,34-MB-Base64-Artefakt: `docs/index.html` ist der mit Abstand größte getrackte Blob, nach zwei Commits liegen bereits zwei nahezu vollständige Kopien in der History. Base64 lässt sich kaum deltifizieren, jeder künftige Build-Commit kostet erneut rund 1,3 MB. Heute unkritisch (`.git` 5,0 MB, size-pack 589 KiB), aber linear wachsend ohne Bremse. Nur relevant, falls `docs/` als Pages-Quelle bleibt.

**F81.** `serve.py` ist gitignored, obwohl die gesamte Verifikations-Doku gegen seinen Port arbeitet (`grafik:883`). Ein frischer Klon hat weder das Werkzeug noch einen Hinweis, und ein normaler `python3 -m http.server` liefert cachende Antworten, also genau das Verhalten, gegen das `serve.py` gebaut wurde. Der Ausschluss ist nirgends begründet, anders als bei `Graphics/` und `assets/cf/`. Kein `README.md` und kein `LICENSE` im Repo-Root. Kosten: Zeile streichen und 542 Bytes committen, oder eine Zeile Begründung.

**F82.** Vier von 15 Commits ohne den sonst durchgängigen `Co-Authored-By`-Trailer (`6c116d0`, `4864154`, `b4a6bef`, `116d164`), darunter die zwei großen Codecommits G2 und G3. Belanglos für sich, aber bei einer Arbeitsweise "pro Phase eine frische Session" ist der Trailer die einzige Spur im Repo, welche Phase von welchem Modell kam. Nicht reparabel ohne History-Rewrite, der nicht zu empfehlen ist.

**F83.** Die W1-Abnahmeliste war nachweislich unvollständig: `showLoading()` zeigte nach `a67c9c3` weiterhin "SuperDuper Adventure" und wurde erst mit `1b47e8e` nachgezogen, aufgefallen auf der Pages-Seite, nicht bei der Abnahme. Behoben, aber der Befund bleibt: die Abnahmeliste ist orts- statt sichtbarkeitsbasiert formuliert und hat deshalb einen Screen übersehen, den jeder Spieler sieht. Derselbe Mechanismus hat F24 durchrutschen lassen, das noch offen ist. Die Umsetzungsnotiz behauptet pauschal "Alle Abnahme-Punkte geprüft". Kosten: nichts am Code. Künftig die Abnahme über sichtbare Bildschirme führen.

---

## Regressionsschutz im Einzelnen

Beide Blöcke (`gameplay:29-44`, `grafik:57-80`), Urteil je Punkt. Selbst am Code nachgeprüft.

| # | Punkt | Urteil | Beleg |
|---|---|---|---|
| 1 | HUD nur über `setTxt`/`setHTML`/`setStyle` mit Dirty-Check | **aufgeweicht** | Dirty-Check korrekt implementiert (`index.html:910-912`), aber zwei `title`-Attribute pro Frame ungeschützt, siehe F29 |
| 2 | Touch-Handler am `window`, `.beltSlot`-Regel, Frame-Watchdog | eingehalten | Zauber- und Angriffs-Drag hängen am `window` (`:6078-6111`, `:6133-6167`), `.beltSlot *{pointer-events:none}` steht (`:28`), Watchdog vorhanden und pro Frame gerufen (`:6179`, `:6201`) |
| 3 | Kein `Math.hypot` in Hot Paths | **aufgeweicht** | 6 Fundstellen im Frame-Pfad, davon 3 neu aus G5, siehe F41 |
| 4 | Pool mit Typ-Tags, keine Closures, keine Allokationen im Renderpfad | **verletzt** | Pool-Teil mustergültig (`drawPool` `:5085-5092`, Komparator hochgezogen); Closures F32, Allokationen F30, F31, F73 |
| 5 | Caps 900 Partikel, 70 Floater | eingehalten | `MAX_PARTICLES = 900` (zur Laufzeit bestätigt), `floaters.length > 70` (`:5075`) |
| 6 | Tot-Guard in `hurtMon()` | eingehalten | `if(m.dead) return;` (`:2598`) mit Begründungskommentar |
| 7 | Sprite-Framezahlen hart im Code | eingehalten | `addSheet(...)` mit expliziten Framezahlen; `index.html:501-503` und `:566-569` halten ausdrücklich fest, dass nicht aus dem Manifest übernommen wird |
| 8 | Minimap einmal gebacken, Blit alle 4 Frames | eingehalten | `bakeMinimap()` bei Levelwechsel (`:1676`, `:3571`), `if((frameNo++ & 3) === 0) renderMinimap();` (`:5319`) |
| 9 | 70ms-Bremse auf Crit- und Sterbe-Sound | eingehalten | `gameT - lastCritSfx > 0.07` (`:2627`), `gameT - lastDieSfx > 0.07` (`:2656`) |
| 10 | Kein `ctx.filter`, Tönung nur über `tintedSheet()` | eingehalten | Alle vier `ctx.filter`-Treffer sind Kommentare (`:4243`, `:5351`, `:5414`, `:5561`), kein einziger Aufruf |
| 11 | Boden-Canvas pro Level einmal gebacken | eingehalten | `floorCanvas` einmal in `initFloorGraphics()`, im Frame nur Blit |
| 12 | Ladeliste datengetrieben, kein ratender Ladecode | eingehalten im Code, **Datenquelle mangelhaft** | `addSheet`-Tabelle mit Pfad, Framegröße, Framezahl, Anker; das G0-Manifest als Quelle ist aber teils falsch, siehe F15 und F43 |
| 13 | Übergangszustand bis G5 erlaubt | eingehalten | Alle `addSheet`-Aufrufe laden aus `cf/`, kein Sunnyside-Pfad mehr im Ladeweg |
| 14 | `pixelated`-Rendering konsistent | **verletzt** | Fehlt an allen geskinnten UI-Elementen, siehe F34; bewusste Ausnahme bei `bakeMinimap`, siehe F72 |

---

## Läuft es

* **Syntaxcheck:** ein einziger Inline-Skriptblock ab Zeile 445, 5856 Skriptzeilen, `new Function(...)` läuft ohne Fehler durch. Keine externen Skripte.
* **Live-Lauf** über `http://localhost:8378/adventure/index.html`: Ladebildschirm, Startbildschirm, "Dienst antreten", Dorf, Kessel-Panel mit allen drei Reitern inklusive "Akten". **Keine einzige Konsolenmeldung**, weder Fehler noch Warnung. 99 von 99 Asset-Requests mit HTTP 200, kein 404.
* **Laufzeit-Introspektion:** `ZUTAT_NOUNS` 21, `ZUTAT_ADJ` 26, `WIRKUNG` 24, `FLUCH` 24, `MONDEF` 21, `BLAETTER` 18, `ZONES` 6, `HINWEISE` 11, `KAM_MOD` 8. Alle Zahlen decken sich mit Plan und Umsetzungsnotizen. `knAssertCaps()` liefert `true`.
* **GitHub Pages:** `https://wurstbrotdlx.github.io/superduper-adventure/` liefert 1372418 Bytes mit sha256 `8886c3de…`, byte-identisch mit `docs/index.html` und `dist/index.html`. Der Live-Stand entspricht exakt dem, was in `main` liegt. `docs/index.html` ist ein exakter Build der aktuellen `index.html`: gleiche Zeilenzahl, einziger Unterschied ist die eingesetzte `ASSET_BLOBS`-Zeile.

---

## Ungeprüft

Ehrlich lückenhaft statt gefällig vollständig.

**Die adversariale Gegenprobe ist ausgefallen.** Der Prüflauf war zweistufig angelegt: erst Prüfung, dann eine unabhängige Instanz je Fund mit dem Auftrag, ihn zu widerlegen. Elf von zwölf Prüfaufträgen sind durchgelaufen, aber alle 80 Gegenproben sind am Sitzungslimit gescheitert. Ich habe daraufhin die neun Funde der Stufe "bricht etwas" sowie F10, F24, F25, F26, F28, F33 und den kompletten Regressionsschutz-Block selbst am Code gegengeprüft; diese sind belegt. **Die übrigen Funde, insbesondere alle unter "kosmetisch", sind Einzelmeldung ohne zweite Meinung.** Vor einer Reparatur lohnt bei jedem einzelnen ein kurzer Blick an die genannte Stelle.

**Abschnitt 5 des Auftrags ist unvollständig abgearbeitet.** Der Prüfauftrag "stillschweigende Abweichungen: alle konkreten Zusagen des Plans (Zahlen, Bezeichner, Tastenbelegungen, Schwellenwerte, Wahrscheinlichkeiten, Cooldowns) einzeln nachrechnen" ist ebenfalls am Sitzungslimit gescheitert. Was in diesem Bericht an stillschweigenden Abweichungen steht, ist Beifang der phasenweisen Prüfung, keine systematische Zusagen-Bilanz. Das ist die größte Lücke des Berichts.

**Laufzeitgebunden, nicht als erfüllt gezählt:**

* Hörbarkeit, Unterscheidbarkeit und Lautstärkeverhältnis der sechs Musikzonen; korrekte Transposition der Stinger; wie stark der Zonenwechsel-Duck aus F49 auffällt; ob der fehlende Tiefpass aus F50 hörbar ist.
* Die 6s/3s-Anzeigedauern der Knöterich-Kanäle, das tatsächliche Nerv-Empfinden der Anti-Nerv-Regeln, das Zusammenspiel von Zettel und Randnotiz im Betrieb (F6 ist statisch belegt, die Häufigkeit nicht gemessen).
* Lösbarkeit aller acht `KAM_MOD`-Module und Esc-Abbruch ohne Softlock. Ich habe keine Kammer durchgespielt.
* Spielbarkeit bei maximaler Fluchkombination.
* Sichtbare Slot-Änderung pro Qualitätsstufe (G2), Fugenfreiheit und Bake-Zeit (G4), UI-Skin auf echten Touch-Geräten, Wetteroptik, Frame-Budget in der Horde gegen die 0,6-ms-Referenz.
* Ob das Schachbrett aus F39 und die Gebäuderänder aus F38 im Spiel als Fehler oder als Gestaltung gelesen werden.
* Der 300-Frame-Soak mit Zaubern. Ich habe das Spiel gestartet und bedient, aber keinen Lasttest gefahren.

**Nicht geprüft:** die tatsächliche GitHub-Pages-Einstellung im Repo (nur die ausgelieferte Datei), der Renderstand auf einem echten Mobilgerät, und die Lizenzfrage aus F9 in der Sache. Ob der Kenmi-Lizenztext das Einbacken in einen öffentlich getrackten Build deckt, ist eine Frage an die Lizenz, nicht an den Code; ich stelle nur den Widerspruch zur eigenen Auslegung im Repo fest.

---

## Vorschlagsliste

Priorisiert, mit Aufwandsschätzung. Nichts davon ist umgesetzt.

### Zuerst, weil es Zustand verliert oder Spiel bricht

1. **F4 Grußpflicht in der Kammer entschärfen.** Eine Zeile. Der einzige Fund, der einen Build unspielbar machen kann.
2. **F1 Dienstsiegel persistent machen.** Etwa 5 Zeilen. Verlorener Fortschritt ist das Schlimmste, was ein Roguelite-Meta anrichten kann.
3. **F3 Musikbusse umhängen.** Fünf Zeilen. "Aus" muss aus heißen.
4. **F5 `dist2` durch `sqDist` ersetzen.** Zwei Bezeichner. Repariert gleichzeitig einen Regressionsverstoß und einen kaputten Hinweis-Trigger.
5. **F2 Guard für `amtFeierabendNehmen`.** Eine Zeile. Sonst ist der dokumentierte Rückschalter faktisch kaputt.
6. **F7 Dorf-Ausschluss in `setzeKammerTueren`.** Eine Zeile, Vorbild steht zwei Funktionen weiter.
7. **F8 `shadowDirt` in `TILE_TINT` zurück.** Eine Zeile.

Zusammen unter einer Stunde. Ein Commit, Betreff etwa `fix: Querstände aus G5 und vier verlorene Zustände`.

### Dann, weil es eine Entscheidung braucht

8. **F9 Lizenzfrage zum Pages-Build entscheiden.** Entweder die Auslegung in `build-single.mjs` und `grafik:227` an die gelebte Praxis anpassen (15 Minuten), oder Pages auf `gh-pages`/Actions umstellen und die History bereinigen (etwa 1 Stunde). Nicht offen lassen: der Widerspruch steht schriftlich im eigenen Repo.
9. **F19 Gold-Doppelbuchung klären.** Entweder die Bank bekommt den einbehaltenen Rest, oder der Bericht wird ehrlich beschriftet. Ein bis zwei Zeilen plus Balancing.
10. **F20 Ausbau-Kosten durchspielen.** Braucht eine echte Schicht am Stück, nicht Rechnen. Halber Abend. Hängt an 9.
11. **F25 Titel-Schreibweise entscheiden.** Erst den Widerspruch zwischen `phase-w1-terminologie.md:17` und `:36` auflösen, dann eine Zeile.

### Dann die Panel- und Register-Nachzüge aus G5

12. **F10 `amtFensterOpen` in die drei Guard-Listen**, plus `knIdleT = 0`, plus Muffle-Entscheidung. Etwa 20 Minuten.
13. **F36 Panel-Dämpfung** für die fünf spielinternen Panels, mit gemeinsamem Zähler. Etwa 45 Minuten, weil das Toggle-Verhalten Sorgfalt braucht.
14. **F33 Schichtnummer plus eins.** Eine Minute.
15. **F11, F12, F13, F14** Knöterich-Nachzüge. Je eine Zeile, zusammen 20 Minuten.

### Dann Doku, in einem Rutsch

16. **F22, F23, F78** Statusmarker und veraltete Planabsätze. W1 und W2 als erledigt markieren, die drei überholten Abschnitte umschreiben, eine Markerkonvention wählen. Etwa eine Stunde, und es ist die Arbeit mit dem besten Verhältnis von Aufwand zu vermiedener Fehlarbeit: `gameplay:244` und `:540` würden W3 sonst direkt in die Irre führen.
17. **F24** Kammerschild: zuerst die falsche Prämisse in `phase-w1-terminologie.md:135` korrigieren, dann die Zeichenkette. Etwa 20 Minuten wegen der Schildbreite.
18. **F44, F79, F81** `.gitignore`-Kommentar, Build-Schritt für `docs/`, Commit-Regel, `serve.py`. Etwa 30 Minuten.
19. **F42, F69, F70, F43, F15** Grafik-Plan und Manifest geraderücken. Etwa eine Stunde, davon der größte Teil für die drei Manifest-Overrides plus Neulauf des Audits.

### Dann Text, gebündelt als eigener Abschnitt

20. **F26, F27, F28, F59, F60, F61, F62** und die Emoji-Fälle aus F14 und F57. Das ist Texterarbeit im Amtsdeutsch-Register und gehört inhaltlich zu W3, nicht in eine W1-Nachbesserung. Erst F25 entscheiden. Halber Abend.

### Zuletzt Performance und Aufräumen

21. **F30, F31** Cache-Keys vorberechnen. Etwa 30 Minuten, der einzige Posten mit messbarem Effekt in der Horde.
22. **F29, F32, F41, F34, F73** restliche Regressionsverstöße. Etwa eine Stunde, alles klein und lokal.
23. **F37, F38, F39, F64** Dorf-Optik. F39 braucht Gegensehen am laufenden Spiel, die anderen drei sind je eine Bedingung. Etwa eine Stunde.
24. **F45, F46, F47, F48, F52, F55, F66, F67, F68** toter und irreführender Code. Etwa 45 Minuten, angenehm nebenbei.
25. **F16, F17, F18** G2- und G3-Restposten. F18 ist eine echte Regression und könnte auch weiter nach vorn; F16 ist ein Abnahmetext, kein Codefehler.

**Nicht empfohlen:** F75 (Vermutungen-Ausbau ist eine gekaufte Kaufentscheidung), F40 (die Trennung der beiden Amtswege ist bewusst und technisch begründet), F72 (die Minimap-Glättung ist richtig), F74 (die Zahl im Zutaten-Tooltip ist kein Verstoß), F82 (kein History-Rewrite für einen Trailer).
